# Serata

Diario personale e gratuito per serie TV e film, progettato per iPad. I dati rimangono nel browser e possono essere esportati come backup JSON.

## Avvio locale

`npm install` e poi `npm run start`.

Il workflow GitHub Pages pubblica ogni aggiornamento inviato al branch `main`.
